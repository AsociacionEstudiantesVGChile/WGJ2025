using UnityEngine;

public class Main : MonoBehaviour {

    [SerializeField] private DayInfo[] _days;

    private void Start() {
        foreach (DayInfo day in _days) {
            
        }
    }
}